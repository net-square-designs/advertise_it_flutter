import 'package:flutter/material.dart';

const appBlack = {
  100: const Color(0XFFD3D4D4), // used for alt text
  200: const Color(0XFFFFFFFF), // used for description text
  300: const Color(0XFF4D4F50), // used for the search bar
  400: const Color(0XFF323539),
};
const appWhite = {
  100: const Color(0XFFFFFFFF), // used for alt text
  200: const Color(0XFFFFFFFF), // used for description text
};

const appPink = Color(0XFFF74F78);
const appBlue = Color(0XFF2D72FF);